# Cloudflare Pages — Next.js static export
# Build command: npm run build
# Output: dist (synced from Next.js out/) so legacy CF settings still work
name = "sellsolar"
compatibility_date = "2025-09-01"
pages_build_output_dir = "dist"
